spring:
  cloud:
    config:
      fail-fast: true
      uri: ${SPRING_CONFIG_URI:http://vbc-config:8888}
      username: admin
      password: s3cret
  config:
    import: "optional:configserver:"
logging:
  level:
    com:
      netflix:
        zuul:
          http:
            HttpServletRequestWrapper: ERROR