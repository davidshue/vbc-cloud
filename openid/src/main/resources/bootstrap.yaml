spring:
  application:
    name: openid-server
  cloud:
    config:
      failFast: true
      uri: ${SPRING_CONFIG_URI:http://vbc-config:8888}
      username: admin
      password: s3cret
  config:
    import: "optional:configserver:"
    use-legacy-processing: true
